import Hero from "../components/Hero.jsx";

const ProcessorView = ({searchParams,setSearchParams}) => {

    const processorId = searchParams.get("processorId");

    return (
        <div className="bg-black">
        </div>
    );
}
export default ProcessorView;